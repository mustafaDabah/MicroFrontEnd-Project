{
  "name": "cart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite build && vite preview --port=5000 --strictPort"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.3.1",
    "vite": "^5.0.8"
  },
  "dependencies": {
    "@originjs/vite-plugin-federation": "^1.3.3"
  }
}
